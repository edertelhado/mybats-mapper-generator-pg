<?xml version="1.0" encoding="UTF-8" standalone="no"?><mapper namespace="com.etelhado.webcoletor.mappers.DepositoMapper"><insert id="insere" keyProperty="codigo" useGeneratedKeys="true">INSERT INTO depositos(descricao,lote_inicial,lote_final) VALUES ( #{descricao}, #{loteInicial}, #{loteFinal})  RETURNING codigo</insert><update id="atualiza">UPDATE depositos SET descricao = #{descricao}, lote_inicial = #{loteInicial}, lote_final = #{loteFinal}WHERE codigo = #{codigo}</update><delete id="deleta">DELETE FROM depositos WHERE codigo = #{codigo}</delete><select id="lista" resultType="com.etelhado.webcoletor.dto.Deposito">SELECT * FROM depositos</select><select id="obter" resultType="com.etelhado.webcoletor.dto.Deposito">SELECT * FROM depositos WHERE codigo = #{codigo}</select></mapper>